<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lthien.hoclichsu.activities" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".GameActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity -->
        <!-- android:name=".GameActivity" -->
        <!-- android:label="@string/title_activity_game" > -->
        <!-- </activity> -->
        <activity
            android:name=".QuestionActivity"
            android:label="@string/title_activity_question" >
        </activity>
        <activity
            android:name=".QuestionChapterActivity"
            android:label="@string/title_activity_image_question" >
        </activity>
        <activity
            android:name=".ChapterDetailActivity"
            android:label="@string/title_activity_chapter_detail" >
        </activity>
    </application>

</manifest>
